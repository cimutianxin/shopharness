# ShopHarness —— 简历项目描述 & 面试 Q&A

> 仓库:https://github.com/cimutianxin/shopharness
> 技术栈:Python 3.13 / Qwen3-8B-FP8 / vLLM / LangGraph / SQLite / OpenAI function calling

---

## 一、简历项目描述

### 详细版(技术简历/项目经历)

**ShopHarness —— 面向电商客服场景的 Agent Harness(个人项目)** 2026.07–2026.08

针对"小参数模型如何在生产客服场景达到可用水平"的问题,设计并实现了一套完整的 Agent Harness(脚手架)系统:以本地部署的 Qwen3-8B-FP8 为基座,通过上下文工程、权限模型、子代理、分层记忆与自进化闭环,将 8B 模型武装成可处理售前转化、售中改价、售后工单全流程的客服 Agent。

- **上下文工程**:设计四层上下文架构(技能指令/记忆/结构化状态/历史)与三级 compaction 机制(工具结果瘦身 → 滑窗事实抽取 → 全量摘要),30+ 轮长会话下关键事实零丢失,token 成本可控
- **工具与权限系统**:基于 OpenAI function calling + MCP 风格 schema 实现 8 个业务工具;设计 READ/WRITE/DANGEROUS 三级权限模型,改价等危险操作需买家复述确认 + 最低限价护栏拦截 + 审计落库,真实模型实测**危险操作零误执行**
- **多 Agent 架构**:实现上下文隔离的子代理(检索/售后),中间检索噪音不进主对话上下文,仅回传结论摘要;售后长流程基于 LangGraph interrupt + SqliteSaver checkpoint,支持跨进程断点恢复
- **记忆与自进化**:实现情景/语义/程序性三层记忆(会话摘要、买家画像、技能版本);构建 bad case 挖掘 → LLM 提案 → 离线回归门禁 → 灰度/回滚的自进化闭环,以及 traces → SFT/DPO 的数据飞轮管线(强制 PII 脱敏)
- **工程质量**:64 项单元测试 + 15 条 trajectory 评测场景全绿;真实 vLLM 端到端验证 6 类会话(含子代理委托、跨会话记忆注入),首 token 延迟 ~1.6s,prefix cache 命中率 77%

### 精简版(一页简历)

**ShopHarness —— 电商客服 Agent Harness(个人项目)** | Python, Qwen3-8B, vLLM, LangGraph
- 设计并实现电商客服 Agent Harness,以本地部署 Qwen3-8B 为基座,覆盖售前转化、售中改价、售后工单全流程,真实模型端到端验证通过
- 实现四层上下文 + 三级 compaction、三级工具权限模型(危险操作复述确认 + 护栏 + 审计)、上下文隔离子代理、LangGraph 长程流程断点恢复
- 构建三层记忆与自进化闭环(bad case 挖掘 → 提案 → 回归门禁 → 灰度回滚),及 SFT/DPO 数据飞轮导出管线;64 项测试全绿

---

## 二、面试 Q&A

### 架构与动机

**Q1:什么是 Harness?为什么不做 prompt 工程微调了事?**
Harness 是包裹在模型外围的工程系统:上下文管理、工具权限、执行引擎、记忆、评测。2025 年后基座模型趋同,差异在 Harness。Prompt 只解决"怎么说",解决不了"8B 模型上下文爆了怎么办""模型被诱导改价怎么办""工具挂了怎么办"——这些必须由系统强制,不能信任模型自觉。

**Q2:为什么选 8B 本地部署,而不是直接调 GPT/Claude API?**
三个原因:成本(客服 7×24 在线,本地推理边际成本近零)、合规(订单/手机号等 PII 不出商家机房)、可控(本地权重可做 SFT/DPO/RL,形成数据飞轮——纯 API 方案永远做不到)。8B 能力弱反而是展示窗口:harness 设计功力全体现在补模型短板上。架构上模型走 OpenAI-compatible 接口,随时可换更大模型。

**Q3:整体架构怎么分层的?**
渠道层(CLI/IM)→ Harness Core(loop/上下文/权限/技能/记忆)→ 工具层(注册表 + 沙箱)→ 推理层(vLLM)→ 数据层(SQLite/记忆/trace)。关键解耦:模型只通过 LLMClient 协议交互,工具只通过 Registry 交互,每层可独立替换。

### 上下文工程(必考)

**Q4:三级 compaction 具体怎么工作?**
1. **工具结果瘦身**:超过阈值的历史工具结果替换为"摘要 + artifact 引用",原文可取回——工具结果(订单 JSON 等)是 token 大头;
2. **滑窗 + 事实抽取**:丢弃中间轮原文前,先让模型抽取关键事实(看中的 SKU、价格承诺、地址)写入 L2 结构化状态——**事实不丢,原文可丢**;
3. **全量摘要**:超长会话整体压缩,只留最近几轮原文。
由 harness 按 token 预算触发,对模型透明,行为可复现、可评测。评测集验证 30+ 轮会话事实保留。

**Q5:L2 结构化状态是什么?为什么不直接摘要?**
L2 是一个 Pydantic 模型(intent/facts/pending),每轮渲染进 system prompt。纯摘要是有损的黑盒文本,结构化状态让"已确认事实"可查询、可断言、可进交接摘要——转人工时坐席看到的就是 L2 + 操作日志,而不是一段含糊的总结。

**Q6:token 怎么计数?**
当前是启发式估算(CJK 1 token/字),接口预留,生产换 AutoTokenizer。估算有偏差没关系——compaction 是阈值触发,只要单调一致即可。

### 工具与安全(必考)

**Q7:怎么防止模型乱调危险工具?**
三道防线,全部在 harness 层强制:
1. **确认门**:DANGEROUS 级工具(改价/退款)在本会话未经买家明确确认前,调用直接拦截,注入提示让模型先复述确认;
2. **护栏 hook**:pre-tool hook 做业务规则校验(改价不得低于商品最低限价),即使模型和买家都"同意"也不能越过商家红线;
3. **审计**:WRITE/DANGEROUS 调用全部落审计表。
实测真实 8B 模型:拦截 → 复述 → 确认 → 执行,以及低于限价被护栏拒绝,全链路零事故。

**Q8:讲讲真实遇到的坑。**
两个。一是模型按货号检索必失败——工具 haystack 没含 SKU 字段,修法是工具补字段而不是指望模型;二是模型编造无效 `category` 参数导致类目过滤后全空,修法是**工具层容错降级**(类目无匹配自动退回全店检索并附 note)。教训:8B 场景下,鲁棒性要内建在工具里,不能假设模型参数一定正确。

**Q9:工具循环失控怎么办?**
最大步数硬上限(超限转人工);同一工具连续失败 2 次熔断;非法工具名注入一次纠正,再犯转人工。所有降级路径的终点都是"带交接摘要的转人工",不是死循环也不是崩溃。

### 多 Agent 与长程执行

**Q10:子代理为什么能省上下文?**
多跳检索的中间产物(几十个候选商品 JSON)是上下文杀手。子代理持独立 ContextManager 跑完整 harness loop,主代理只收到 150 字结论摘要——检索噪音物理隔离。测试断言:主上下文的 tool 消息里没有子代理的 search_products。

**Q11:为什么售后流程用 LangGraph 而主对话不用?**
主对话是短平快的"决策-工具"循环,自研 loop 换来对 compaction 触发点、权限拦截的完全掌控;售后是跨小时/跨天的有状态流程,需要 interrupt(等买家确认)和 checkpoint(进程重启恢复)——这正是 LangGraph 的强项,用 SqliteSaver 落 SQLite,实测 kill 进程后从挂起点恢复。扬长避短,不为用而用。

### 记忆与自进化(差异化亮点)

**Q12:三层记忆怎么分工?**
- **情景记忆**:每次会话结束的摘要,按买家沉淀,下次会话注入 L1;
- **语义记忆**:买家画像(价格敏感/关注品类/投诉史),会话结束用确定性规则从 L2 事实蒸馏;
- **程序性记忆**:技能文件本身——版本化存 skill_versions 表,可灰度可回滚。
关键设计:画像更新是"追加去重"而非覆盖,防止一次异常会话污染长期记忆。

**Q13:自进化闭环怎么防止越改越差?**
自动化只到"提案"为止:bad case 挖掘(规则扫 trace:handoff/护栏拒绝/熔断聚集)→ LLM 生成技能改进提案 → **离线回归门禁**(15 条评测场景全过才放行)→ 灰度上线(技能版本化)。门禁不过自动回滚。自进化 ≠ 放任系统自改,这是工程化和玩具 demo 的分水岭。

**Q14:数据飞轮具体怎么转?**
traces(JSONL span,字段对齐 OTel GenAI 约定)→ 筛选"成功会话"(无 handoff/无护栏拒绝)导出 SFT 样本;同 prompt 下"成功回复 vs 转人工前回复"配对导出 DPO 偏好对 → 强制 PII 脱敏(手机号/身份证/邮箱)+ schema 校验 → 接 ms-swift/TRL 做 LoRA SFT→DPO→(进阶)GRPO,reward 用转化率/轮次效率/违规率的规则组合。

### 评测与工程

**Q15:Agent 没有标准答案,怎么评测?**
评**轨迹**不评单条输出:15 条脚本化场景,断言工具调用序列(子序列匹配)、事件流(拦截/护栏/熔断/compaction 是否触发)、数据库终态(订单金额是否真改了)、L2 事实保留。`--gate` 模式输出退出码,既是 CI 门禁也是自进化的放行闸。

**Q16:可观测性怎么做?**
轻量 JSONL tracer,span 字段名对齐 OTel GenAI 语义约定(gen_ai.system、token 用量、工具参数摘要)——现在零依赖能跑,将来换 OTel SDK + Langfuse 只改 Tracer 一处,span 数据本身无缝迁移。trace 同时就是数据飞轮的原料,一份投入两处产出。

**Q17:如果让你继续迭代,下一步做什么?**
1. Agentic RL:奖励服务 + 轨迹采样接口已在设计上预留,用 GRPO 优化多轮工具调用;
2. 工具层迁到 FastMCP 独立进程(Streamable HTTP),接 A2A 与商家 ERP Agent 互通;
3. pgvector 语义检索替换关键词检索;
4. 子代理并行化(多个检索子代理并发,结果仲裁)。

### 行为面

**Q18:这个项目最大的收获?**
认知转变:Agent 工程的重心不是"让模型更聪明",而是"假设模型会犯所有可能的错,然后系统性兜底"。8B 模型是最佳老师——它犯的每个错(编参数、忽略指令、答非所问)都逼出一个 harness 机制,而这些机制换上 70B 模型依然全部有用。

**Q19:为什么选电商客服场景?**
它是 harness 价值的最佳试炼场:长上下文压力(几十轮会话)、强工具依赖(查/改/算)、可量化北极星指标(询单转化率、转人工率)、清晰的安全红线(改价涉钱)。每个 harness 机制都能对应到一个真实的业务痛点,而不是为了技术而技术。
